import React from "react";
import { Col, Row } from 'react-bootstrap';

export const StakingCard = ({title}) => {
    // Attributes
    // Context
    // Methods
    // Component
    return(
        <>
        Titulo: {title}
        </>
    );
}